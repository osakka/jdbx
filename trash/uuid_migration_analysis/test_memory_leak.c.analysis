File: /opt/jsondb/tests/memory/test_memory_leak.c
=

52-        
53-        if (result) {
54:            json_value_t* id_val = json_object_get(result, "_id");
55-            if (id_val && id_val->type == JSON_STRING) {
56-                doc_id = strdup(id_val->value.string);

PATTERN ANALYSIS:
=================

READ PATTERNS (accessing _id):
54:            json_value_t* id_val = json_object_get(result, "_id");

WRITE PATTERNS (setting _id):

QUERY PATTERNS (searching by _id):

SCHEMA PATTERNS (defining _id):
