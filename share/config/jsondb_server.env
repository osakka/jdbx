# JSONdb Server Environment Configuration
# v2.0.0 Binary Persistence Release
# 
# This file contains environment variables for the JSONdb server.
# The runtime script will automatically load this configuration.

# ====================================================================
# CORE SERVER CONFIGURATION
# ====================================================================

# Server network configuration
JSONDB_PORT=5000
JSONDB_HOST=0.0.0.0

# Logging configuration
JSONDB_LOG_LEVEL=info          # Options: error, warn, info, debug, trace
JSONDB_VERBOSE=false

# ====================================================================
# BINARY PERSISTENCE CONFIGURATION (v2.0.0)
# ====================================================================

# Database file with binary format (.jdb extension)
JSONDB_DB_DIR=/opt/jsondb/build/var/database.jdb

# RBAC configuration file
JSONDB_RBAC_FILE=/opt/jsondb/build/var/rbac.json

# Server operation files
JSONDB_LOG_FILE=/opt/jsondb/build/var/jsondb.log
JSONDB_PID_FILE=/opt/jsondb/build/var/jsondb.pid

# ====================================================================
# DIRECTORY PATHS
# ====================================================================

# Web interface root directory
JSONDB_WEB_ROOT=/opt/jsondb/share/htdocs

# JavaScript integration directories
JSONDB_VALIDATORS_DIR=/opt/jsondb/build/var/validators
JSONDB_TRANSFORMS_DIR=/opt/jsondb/build/var/transforms

# Metrics and monitoring
JSONDB_METRICS_DIR=/opt/jsondb/build/var/metrics

# ====================================================================
# ADVANCED CONFIGURATION
# ====================================================================

# Thread pool configuration (if supported)
JSONDB_MAX_CONNECTIONS=100
JSONDB_THREAD_POOL_MIN=4
JSONDB_THREAD_POOL_MAX=16

# Binary persistence options
JSONDB_BINARY_FORMAT=1                    # Force binary format (auto-detection by default)
JSONDB_BINARY_SIZE_THRESHOLD=10485760     # Size threshold for auto-detection (10MB)

# ====================================================================
# SSL/TLS CONFIGURATION
# ====================================================================

# SSL/TLS options (if enabled)
JSONDB_USE_SSL=false
JSONDB_SSL_CERT_FILE=/opt/jsondb/build/var/ssl/server.crt
JSONDB_SSL_KEY_FILE=/opt/jsondb/build/var/ssl/server.key

# ====================================================================
# DEBUG AND DEVELOPMENT
# ====================================================================

# Debug mode (set to true for development)
JSONDB_DEBUG_MODE=false

# Individual debug categories (when debug mode is enabled)
JSONDB_DEBUG_SOCKET=false
JSONDB_DEBUG_RBAC=false
JSONDB_DEBUG_API=false
JSONDB_DEBUG_REQUEST=false
JSONDB_DEBUG_AUTH=false
JSONDB_DEBUG_PERSISTENCE=false

# ====================================================================
# ENVIRONMENT PATHS (usually auto-detected)
# ====================================================================

# Base directories (override only if using custom installation)
# JSONDB_BASE_DIR=/opt/jsondb
# JSONDB_BUILD_DIR=/opt/jsondb/build
# JSONDB_VAR_DIR=/opt/jsondb/build/var
# JSONDB_SHARE_DIR=/opt/jsondb/share

# ====================================================================
# QUICKJS INTEGRATION
# ====================================================================

# QuickJS library path (auto-detected if installed in /opt/qjs)
# LD_LIBRARY_PATH=/opt/qjs/lib/quickjs

# JavaScript execution timeout (in milliseconds)
JSONDB_JS_TIMEOUT=5000

# Maximum JavaScript memory usage (in bytes)
JSONDB_JS_MAX_MEMORY=16777216    # 16MB

# ====================================================================
# NOTES
# ====================================================================
# 
# 1. Binary Format: v2.0.0 automatically uses the high-performance
#    binary format for optimal performance (4-6x faster than JSON)
#
# 2. File Extensions: Database files now use .jdb extension to 
#    indicate binary format
#
# 3. Auto-detection: The server automatically detects the best
#    format based on database size and content
#
# 4. Performance: For best performance, ensure adequate disk space
#    and memory for the binary persistence system
#
# 5. Compatibility: All existing JSON data is automatically migrated
#    to binary format on first startup
#
# ====================================================================