// Simple JavaScript test file for the JSON Database Server

// Define a simple math operation function
function mathOperations(a, b) {
    return {
        sum: a + b,
        difference: a - b,
        product: a * b,
        quotient: a / b
    };
}

// Call the function with two numbers
mathOperations(10, 5);