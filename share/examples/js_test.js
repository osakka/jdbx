// Simple JavaScript test function
function testMath(x, y) {
    return {
        sum: x + y,
        product: x * y,
        difference: x - y,
        quotient: x / y
    };
}

// Export the test function
testMath(10, 5);